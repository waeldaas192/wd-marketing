name: UI and build audit
on:
  push:
    branches: ['feature/**']
  pull_request:
    branches: [main, feature/global-experience-v1]
permissions:
  contents: read
concurrency:
  group: ui-audit-${{ github.event.pull_request.head.ref || github.ref_name }}
  cancel-in-progress: true
jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      NEXT_TELEMETRY_DISABLED: '1'
      CONTACT_ENABLED: 'false'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - name: Snapshot audited source
        run: git archive --format=tar.gz --output=/tmp/wd-source.tar.gz HEAD
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Contact validation and mocked provider tests
        run: node qa/contact-unit.cjs
      - name: Verify original Lucide subset provenance
        run: node qa/icon-source.cjs
      - run: npx tsc --noEmit
      - run: npm run build
      - name: Install isolated browser audit tools
        run: |
          npm install --prefix /tmp/wd-ui-audit --no-save --package-lock=false playwright@1.58.2 @axe-core/playwright@4.11.1 esbuild@0.25.12
          /tmp/wd-ui-audit/node_modules/.bin/playwright install --with-deps chromium
      - name: Audit production preview
        env:
          QA_NODE_MODULES: /tmp/wd-ui-audit/node_modules
        run: |
          node qa/gallery-fixture.cjs
          npm run start > /tmp/wd-preview.log 2>&1 &
          SERVER_PID=$!
          trap 'kill "$SERVER_PID" || true' EXIT
          for attempt in $(seq 1 60); do
            if curl --fail --silent http://localhost:3000/ > /dev/null; then break; fi
            sleep 2
          done
          node qa/browser-audit.cjs
          node qa/completion-browser.cjs
          node qa/section-motion.cjs
          node qa/fluid-hero.cjs
          node qa/ripple-merge.cjs
          node qa/connection-bead.cjs
          node qa/ripple-demo.cjs
          node qa/studio-theme.cjs
          node qa/library-icons.cjs
          node qa/faq-scroller.cjs
      - name: Preserve audit evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ui-audit-evidence
          path: |
            qa-results/
            /tmp/wd-preview.log
            /tmp/wd-source.tar.gz
          if-no-files-found: ignore
          retention-days: 7
