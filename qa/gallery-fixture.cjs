// Isolated browser harness for the real gallery component. next/image is a test adapter.
const path=require('node:path'),fs=require('node:fs');
const root=process.cwd(),out=path.join(root,'qa-results');fs.mkdirSync(out,{recursive:true});
const {build}=require(path.join(process.env.QA_NODE_MODULES,'esbuild'));
const pixel='data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mP8/x8AAwMCAO+jH5kAAAAASUVORK5CYII=';
build({stdin:{contents:`import React from 'react';import {createRoot} from 'react-dom/client';import {CaseGallery} from './src/components/ui/CaseGallery';import {MediaFrame} from './src/components/ui/MediaFrame';const pixel=${JSON.stringify(pixel)};const items=[{src:pixel,originalSrc:'/images/a.webp',alt:'Fixture A',width:800,height:500,ready:true},{src:pixel+'#second',originalSrc:'/images/b.webp',alt:'Fixture B',width:800,height:500,ready:true},{src:'/images/later.webp',originalSrc:'/images/later.webp',alt:'Imagery later',width:800,height:500,ready:false}];createRoot(document.getElementById('root')).render(<><CaseGallery images={items} project='Gallery test'/><div id='broken-fixture'><MediaFrame asset={{...items[0],src:'data:image/png;base64,broken'}} label='Failed load'/></div></>);`,resolveDir:root,loader:'tsx'},bundle:true,platform:'browser',format:'iife',jsx:'automatic',define:{'process.env.NODE_ENV':'"production"'},outfile:path.join(out,'gallery-fixture.js'),plugins:[{name:'test-image-adapter',setup(builder){builder.onResolve({filter:/^next\/image$/},()=>({path:'image',namespace:'test-image'}));builder.onLoad({filter:/.*/,namespace:'test-image'},()=>({loader:'tsx',resolveDir:root,contents:`import React from 'react';export default function Image({src,alt,onError,className,fill}){return <img src={src} alt={alt} onError={onError} className={className} style={fill?{position:'absolute',inset:0,width:'100%',height:'100%'}:undefined}/>}` }));}}]}).catch(error=>{console.error(error);process.exitCode=1;});
