import type { Metadata } from "next";
import Image from "next/image";
import { notFound } from "next/navigation";
import { insights, getInsight } from "@/data/insights";
export function generateStaticParams(){return insights.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params; const item=getInsight(slug); return item?{title:item.title,description:item.summary}:{};}
export default async function InsightPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const item=getInsight(slug); if(!item)notFound(); return <article className="article-shell"><div className="article-head container"><p className="eyebrow">{item.category} · {item.read}</p><h1>{item.title}</h1><p>{item.summary}</p><div className="mt-10 overflow-hidden rounded-[28px] border border-white/10"><Image src={item.image} alt={item.imageAlt} width={1600} height={1000} className="h-auto w-full" priority /></div></div><div className="article-body"><h2>The principle</h2><p>Marketing activity should be prioritised by the business outcome it can influence, not by the visibility of the metric. That means starting with intent, economics, offer quality and the path from attention to enquiry.</p><h2>What to measure</h2><p>Track the actions closest to commercial value: qualified enquiries, booked calls, quote requests, sales-stage progression and revenue where available. Rankings, clicks and sessions remain useful diagnostic signals, but they are not the finish line.</p><h2>How to apply it</h2><p>Identify the highest-value customer problem, map the demand around it, build the strongest page for that intent, connect acquisition, and instrument the full journey. Then use evidence from lead quality and conversion behaviour to decide what deserves more investment.</p><div className="article-note">Editorial starter content. Expand this article with verified project examples, screenshots and original data before publication.</div></div></article>}
